# ── 依赖 ──
node_modules

# ── 构建产物 ──
dist
out
build
*.tsbuildinfo
.eslintcache

# ── 自动生成的类型声明 ──
src/renderer/auto-imports.d.ts
src/renderer/components.d.ts

# ── 环境变量（可能含敏感信息） ──
.env
.env.local
.env.*.local
*.local

# ── 用户数据目录（运行时生成，不要提交测试数据） ──
.little-q-desktop/

# ── 编辑器 / IDE ──
.idea
.codegraph
.vscode
*.swp
*.swo
*~

# ── 操作系统 ──
.DS_Store
Thumbs.db
Desktop.ini

# ── 日志 ──
*.log
logs

# ── 其他包管理器生成的锁文件 ──
package-lock.json
